[**summit-kit**](../../README.md)

***

[summit-kit](../../modules.md) / [server](../README.md) / Icon

# Function: Icon()

> **Icon**(`name`): `Element`

Defined in: react/server/Icon/index.tsx:22

Renders an icon component based on the provided `name` prop.

## Parameters

### name

`IconProps`

The name of the icon to render. Must correspond to a key in the `Icons` object.

## Returns

`Element`

The corresponding icon component if found, otherwise a fallback message.
